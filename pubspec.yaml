name: solveig_identity_app
description: Solveig Identity reference mobile client for solveig-api-platform /v1 APIs.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^16.0.0
  http: ^1.4.0
  shared_preferences: ^2.5.3
  image_picker: ^1.1.2
  qr_flutter: ^4.1.0
  uuid: ^4.5.1
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
